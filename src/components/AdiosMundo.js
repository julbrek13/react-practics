//esta seria la manera correcta, un componente por archivo js
//ahora si podremos poner el default ya que es el default del archivo
import React from "react";
export default function AdiosMundo(){

    return(<div>
        <h3>Adios</h3>
    </div>);
}